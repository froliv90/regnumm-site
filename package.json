{
  "name": "regnumm-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site institucional do Regnumm — regnumm.com.br",
  "scripts": {
    "build": "npm run limpar && eleventy",
    "dev": "eleventy --serve",
    "limpar": "node -e \"require('fs').rmSync('_site',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "markdown-it-footnote": "^4.0.0"
  }
}
